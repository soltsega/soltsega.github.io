title: "Machine Leaning Blog"
description: "Mathematical insights into deep learning"
url: "https://soltsega.github.io"

theme: minima

markdown: kramdown
kramdown:
  math_engine: mathjax

plugins:
  - jekyll-feed

# Make MathJax available to all pages via a custom head include
mathjax: true

# Hide automatic page links in the top header (removes the redundant title)
header_pages: []
